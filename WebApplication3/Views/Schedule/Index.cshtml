@model IEnumerable<MVCEmployeeScheduler.Models.Schedules>

@{
    ViewBag.Title = "Index";
}

<h2>Schedules</h2>

<p>
    @Html.ActionLink("Add more schedules", "../Manager")
</p>
    @Html.ActionLink("Manage off days", "../OffDaysForm/")

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmpId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTimeFrom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTimeTo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmpId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTimeFrom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTimeTo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.EmpId }) |
            @Html.ActionLink("Details", "Details", new { id = item.EmpId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmpId }) |
            @*@Html.ActionLink("Manage off days", "../offdays/Index", new { id = item.EmpId })*@
        </td>
    </tr>
}

</table>
